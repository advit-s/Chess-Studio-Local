# Chess Studio Local 0.3.0 requirement-to-evidence checklist

Updated: 2026-07-14. This checklist was rebuilt from the complete 1,184-line
canonical prompt (`Pasted text (2).txt`, SHA-256
`a30e22ff93e2900be009a1f65e8516f8b692bc2bcd71ce5d4f30a3b1bd27f598`).
“Verified” means an output comparison or real-browser interaction exists; a
page merely rendering or a worker merely loading is not counted.

## Release, installation, dependencies, and attribution

| Requirement | Relevant file or subsystem | Current state | Planned fix | Test or manual verification | Evidence/result | Remaining limitation |
| --- | --- | --- | --- | --- | --- | --- |
| REL-01: v0.3.0 in package, lock, README, manifest, UI, reports, caches, and release filename | `package*.json`, manifest, `App.tsx`, docs, `sw.js` | Verified | Normalized every release surface and added an accessible About/version control | Source search; production browser manifest fetch/UI assertion | Package/lock/manifest/UI report `0.3.0`; core E2E passes | Historical changelog correctly retains 0.2.0 |
| REL-02: truthful prebuilt workflow and verified `dist` | Vite, README, launchers, `dist/**` | Verified | Include a clean production build and remove obsolete hashed assets | `npm run build`; file inventory | 23 files; one CSS, one main JS, one lazy scanner JS; about 25 MB including model/engine | Build must be regenerated after source changes |
| INST-01: deterministic `npm ci`, no custom GitHub model download | `prepare-engine.mjs`, integrity scripts | Verified | Verify bundled Stockfish/OCR assets locally; fail precisely on missing/tampered files | Four install-integrity tests; final clean extraction `npm ci` | Missing path, size/hash tamper, and path escape are rejected | npm registry is needed only to install development dependencies |
| DEP-01: one OCR runtime; remove ONNX | package/Vite/public/docs | Verified | Retained vendored TFJS 0.15.3; removed ONNX dependency, WASM, copying, config, and stale runner | Dependency/source/release inventory | No ONNX production asset or package remains | Legacy TFJS retained for exact frozen-model compatibility |
| DOC-01: documentation matches implementation | README, model README, benchmark README, changelog/report | Verified | Replaced nonexistent ONNX/training claims with exact local TFJS worker contract and limitations | Cross-file source search/review | Docs name nodes, shapes, class order, preprocessing, hashes, backend, caches, and benchmark failures | Documentation cannot substitute for a larger OCR dataset |
| LIC-01: redistributed runtime/model/fixture notices and copies | `THIRD_PARTY_NOTICES.md`, `THIRD_PARTY_LICENSES/**` | Verified | Added TFJS Apache-2.0, model MIT, React MIT, DejaVu licence; retained Stockfish, chess.js, Playwright | Hash comparison with installed/upstream licence text; release inventory | Exact copies and pinned model revision are present | Development-only transitive notices remain in npm package metadata |
| LIC-02: no proprietary Chessis/Chess.com/Lichess assets or code | UI and fixtures | Verified | Used original UI, Unicode/DejaVu generated fixtures, and the MIT upstream reference only | Fixture manifest/licence review | Every fixture records source, licence, and independence | Style coverage is therefore limited |
| LAUNCH-01: Windows/local launchers use included build | `.bat`, `.sh`, `serve-dist.mjs` | Verified with environment limitation | Prebuilt launcher checks `dist`, prefers Node server, finds Chrome, and falls back to Python | Local server protocol/MIME/header probes; clean-extraction launch | Shared server serves the verified production build without npm | Native `.bat` execution is not possible in the Linux verifier; Windows click-through remains recommended |

## Image intake, detection, crop, and homography

| Requirement | Relevant file or subsystem | Current state | Planned fix | Test or manual verification | Evidence/result | Remaining limitation |
| --- | --- | --- | --- | --- | --- | --- |
| IMG-01: accept PNG/JPEG/WebP upload, paste, and drop; reject spoofed/unsupported data | `scanImage.ts`, `ScanPanel`, OCR E2E | Verified | Validate MIME plus magic bytes and route every intake through one decoder | Unit signatures; real production-worker E2E for all five paths; SVG rejection | All supported paths return the exact upstream FEN; SVG/manual state preservation passes | Animated formats and HEIC are intentionally unsupported |
| IMG-02: safe size, decoded-pixel, working-memory, timeout, EXIF handling | `scanImage.ts` | Verified with narrow evidence | 10 MB file cap, 32 MP decoded cap, 4 MP/2048 working cap, 15 s decode timeout, `imageOrientation: from-image` | Dimension/header unit tests; Chromium decode paths | Unsafe dimensions fail before pixel extraction; large safe images are proportionally reduced | No dedicated EXIF-labelled fixture; browser API behavior is relied upon |
| DET-01: evidence-based board detection without random centered fallback | `boardDetection.ts`, OCR core | Verified | Score grid/alternation/aspect/edge signals; return manual-needed when confidence is low | Generated detector units; nine-case benchmark | 9/9 labelled cases detected; category IoU reported | Detection benchmark is small and digital-only |
| DET-02: rotated/perspective boards and labelled IoU | detector/benchmark | Verified | Added rotation-aware candidate refinement and quadrilateral IoU | Rotation and perspective fixtures | 1.5° IoU 99.14%; perspective IoU 98.40% in latest run | Larger rotations/physical photos are unproven |
| WARP-01: true projective homography, bilinear sampling, degeneracy/clamping | `boardDetection.ts`, OCR core/tests | Verified | Solve/invert a 3×3 homography and sample safely | Deterministic checkerboard/unit transforms; perspective benchmark exact FEN | Perspective fixture warps to exact expected position | Extreme/occluded quadrilaterals still require manual work |
| CROP-01: object-fit letterbox mapping and current pointer-up corners | `ScanPanel.tsx` | Verified | Map through contained-image rectangle and use synchronous corner ref on pointer-up | Unit coordinate tests; trusted mobile touch crop E2E | Touch moves labelled corner; warped board/source remain; no stale-corner invocation | Crop handle movement is intentionally bounded to the decoded image |
| CROP-02: four handles usable by mouse, touch, and keyboard | crop overlay/UI | Verified | Pointer capture plus focusable labelled handles and Arrow/Shift+Arrow movement | Mobile CDP touch test; role/focus source review | Trusted touch drag passes and triggers a new warp | Screen-reader spatial interpretation is inherently limited for a 2D crop |

## OCR model contract and worker lifecycle

| Requirement | Relevant file or subsystem | Current state | Planned fix | Test or manual verification | Evidence/result | Remaining limitation |
| --- | --- | --- | --- | --- | --- | --- |
| MODEL-01: locally bundled, pinned, hashed browser model | model assets/integrity scripts | Verified | Pin v1.0.0 revision `c750…bef6`, seven files, sizes/hashes | `verify:ocr-assets`; clean install | All files match `model-integrity.json`; no external inference request | Model/runtime total is about 18 MB |
| MODEL-02: exact upstream 256×256 preprocessing/tile order | `ocrModelContract.ts`, generated worker core | Verified | First channel 0–255, no normalization, file-major `a8..a1…h8..h1`, `[64,1024]` | Contract/tile-order units; exact reference FEN | Known pixels and board FEN match upstream | Model is specialized to its training styles |
| MODEL-03: exact nodes/shapes/classes | contract/worker | Verified | Inputs `Input`, scalar `KeepProb=1`; outputs `probabilities[64,13]`, `prediction[64]`; 13 pinned classes | Contract tests plus real inference | Reference returns all 64 correct classes | None beyond model quality |
| SCORE-01: validate finite scores/logits/probabilities; never invent confidence | worker/UI/history | Verified | Validate row shape/range/sums; nullable scores for class-only output; label as model score; clear scores when a manual edit changes the predicted class | Model-contract units and real-worker correction E2E | No “Confidence” or synthetic 100%; manually changed square reports score unavailable | Model score is uncalibrated and not certainty |
| LIFE-01: separate worker, request IDs, stale/unmount guards, timeout, cancellation | `OcrWorkerClient`, `ocrWorkerState`, scan worker | Verified | Generation/request state, transferable buffers, invalidation, 30 s timeout, dispose guards; manual mutations cancel/invalidate automatic work | Client/state unit races; image replacement and real-worker late-result E2E | Old results cannot overwrite a newer crop/image/manual correction or unmounted panel | Running TF compute is invalidated, not pre-empted mid-kernel |
| LIFE-02: restart, load retry, recovery after model failure | OCR client/UI/worker | Verified | Terminate/recreate worker and reset sticky load promises; keep manual mode | Failure-before-cache E2E, retry/state units | Genuine offline load failure preserves UI and permits analysis via manual FEN | Repeated network/storage failure still requires user action |
| LIFE-03: CPU backend works in a dedicated worker | `scanWorker.js` | Verified | Add worker-safe OffscreenCanvas shim and explicitly select CPU | Real production Chromium exact-FEN test | Reference OCR completes in worker without DOM/WebGL crash | CPU inference is slower than a modern converted model |

## Scanner state, FEN, correction, history, and integration

| Requirement | Relevant file or subsystem | Current state | Planned fix | Test or manual verification | Evidence/result | Remaining limitation |
| --- | --- | --- | --- | --- | --- | --- |
| STATE-01: failure preserves source, corners, warp, edits, and draft | ScanPanel state | Verified | Stage-specific non-destructive errors and explicit Remove Image | Invalid-file and genuine model-failure E2E | Image/canvas/manual board/FEN survive failure; no black screen | Browser tab/process crashes are outside app recovery |
| ORI-01: image orientation, editor view, canonical grid, and FEN are separate | orientation helpers/UI | Verified | Canonical a8–h1 grid; one explicit 180° reinterpretation; independent view flip | Table-driven unit and black-bottom benchmark | Black position is rotated once, never mirrored/double-reversed | Orientation confirmation remains manual and honest |
| FEN-01: generated FEN and user draft do not overwrite each other | ScanPanel/FEN validation | Verified | Dirty draft with explicit Validate, Apply, Reset | Browser typing/apply/analyze test | User text remains until explicit action | None |
| FEN-02: syntax/hard errors versus advisory plausibility warnings | `fenValidation.ts` | Verified | Reject malformed boards/kings/adjacency; warn on pawn/material/position plausibility | Focused validation units | Impossible automatic results are not silently presented as success | Full reachability proof is not attempted |
| FEN-03: side/castling/en-passant/clocks conservative and editable | scanner FEN controls | Verified | Defaults `w - - 0 1`; never infer hidden state from pixels | Controls/round-trip E2E | Exact options feed final FEN; castling warning visible | User must supply non-board fields when known |
| EDIT-01: select/place/replace/remove/move/clear/restore, undo/redo, flip | manual grid reducer/UI | Verified | Palette/two-tap move/eraser/history/baseline restore | Exact manual-FEN E2E and undo/redo/clear E2E | Replacement/movement/erasing produce expected FEN | Manual legality warnings remain advisory so arbitrary editor positions are possible |
| EDIT-02: desktop, touch, keyboard correction with labels/focus | scanner editor/crop UI | Verified | Use semantic buttons/tap workflow; no native DnD dependency; labelled crop controls | Desktop E2E plus mobile `tap()`/trusted touch crop | Mobile manually creates exact `4k3/.../4K3 w - - 0 1` | Full automated WCAG audit was not performed |
| INT-01: analyze, play both colours, board editor, archive, copy | ScanPanel/App integration | Verified | Wire explicit callbacks; engine color is opposite chosen player color; truthful save | Integration E2E | Analyze FEN exact; Play White→engine black, Play Black→engine white; editor focused; archive saved | Clipboard permissions vary by browser context |
| HIST-01: versioned minimal scan records and optional original | `scanHistory.ts`, IndexedDB UI | Verified | Schema v2, corrected crop/grid/FEN/options/scores/model/date/notes; source opt-in | Migration/quota units and browser save/reopen | Original defaults off; history restores and reruns production OCR | Browser quota/profile scope varies |
| HIST-02: transaction completion, quota/error truth, delete/clear/migration | history/storage helpers | Verified | Await transaction results; map quota; never toast success on failure | Storage/history units; integration save assertions | Committed saves report success; failures return typed errors | Browser-specific eviction cannot be prevented |

## Benchmark and measured OCR behavior

| Requirement | Relevant file or subsystem | Current state | Planned fix | Test or manual verification | Evidence/result | Remaining limitation |
| --- | --- | --- | --- | --- | --- | --- |
| BENCH-01: benchmark calls production recognition and strict mismatch fails | benchmark scripts/worker | Verified | Use generated production OCR core and `scanWorker.js`; compare all 64 labels/FEN | Strict run plus negative-control unit | Strict command exits 1 for 3 wrong generated FENs and names wrong squares | A strict all-green model benchmark is not claimed |
| BENCH-02: separate independent/generated/transformed evidence | manifest/report | Verified | Four explicit categories, including upstream-reference separated from independent | Manifest schema/category summary | Independent 0; upstream 1; generated 4; transformed 4 | No independent-real accuracy can be calculated |
| BENCH-03: trusted metadata and ground truth | `images/manifest.json` | Verified | Source/licence/corners/orientation/64 classes/board FEN/known complete FEN per case | Manifest consistency/hash tests | 9 cases validate; expected FEN is never copied from prediction | Ground truth is small and manually curated |
| BENCH-04: scale/rotation/perspective/compression/panels/coordinates/light-dark regressions | fixture generator | Verified | Deterministic transformations and application captures | Generator hashes and benchmark | 4/4 transformed exact; generated themes/pieces measured separately | Transform siblings are not independent evidence |
| BENCH-05: required metrics and expected-vs-detected output | results JSON/report | Verified | Detection/IoU/orientation/square/occupied/empty/exact/wrong squares/load/inference/RSS | Latest `results/latest.json` | All metrics present per case and category | RSS includes Node worker/process, not precise browser heap |
| BENCH-06: honest measured accuracy | benchmark report | Limited by model/dataset | Preserve failures; require manual correction rather than changing labels | Latest strict/diagnostic run | Upstream 1/1 exact; generated 1/4 exact (87.11% mean square); transformed 4/4 exact; detection 9/9 | No universal, physical-board, or production-level OCR claim |

## Core chess, engine, archive, and regressions

| Requirement | Relevant file or subsystem | Current state | Planned fix | Test or manual verification | Evidence/result | Remaining limitation |
| --- | --- | --- | --- | --- | --- | --- |
| CORE-01: click, mouse drag, touch move, legal highlights, illegal rejection | board/game reducer | Verified | One position-token move contract and pointer capture | Real Chromium 20-ply, native drag, CDP touch, illegal click | Core E2E all pass; board remains visible | None |
| CORE-02: castling, promotion, en passant, check/mate/stalemate/draw rules | chess.js/game helpers | Verified | Preserve chess.js legality and expose status safely | Unit rule tables plus browser special moves/checkmate | All focused rules pass | chess.js is the legal-rules authority |
| CORE-03: undo/redo, flip, navigation, new game, custom FEN | immutable game document/App | Verified | Reconstruct positions from canonical start + moves; stale expected-FEN guard | Reducer units and core E2E | Navigation/custom-FEN/undo/redo/flip survive 20 plies | None |
| CORE-04: PGN round-trip headers/result/comments/NAGs/variations/SetUp/FEN | `gameState.ts`, import/export | Verified for supported source-preserving edits | Preserve original annotation tokens while applying supported mainline edits; emit SetUp/FEN | Exact token unit tests and browser import/export/navigation | Supported annotations survive and source is not silently destroyed | UI is not a general variation-tree editor |
| ENG-01: local Stockfish analysis, MultiPV, mate, timeout/restart | `StockfishClient`, UCI parser/panel | Verified | Handshake/readiness, serialized transitions, FEN+generation identity, legal bestmove checks | UCI units, Node smoke, production E2E | Real Stockfish returns legal lines and smoke best move `e2e4` | Lite single-thread strength/performance |
| ENG-02: cancellation/no stale output/no duplicate reply | engine client/App/review | Verified | Stop/drain/restart generations and mode ownership | Rapid depth/MultiPV/new-position E2E; play/review tests | Old output cannot crash/change new position; exactly one play reply | Very long soak testing not performed |
| CORE-05: play as White/Black and full-game analysis | App/review/scanner integration | Verified | Correct color handoff and cancellable sequential review | Core and scanner integration E2E | Both scan play colors and cancel/restart review pass | Review speed depends on configured depth/hardware |
| STORE-01: archive save/load and settings persistence with truthful failures | storage/App/archive | Verified | Validate/migrate inputs and return write status | Storage units and refresh/archive E2E | Valid records persist; success only follows committed write | Saved games remain localStorage-capped/profile-local |
| REG-01: no post-move black screen, square/fullscreen sizing, error boundary | engine panel/board CSS/ErrorBoundary | Verified | Guard PV conversion, clear stale snapshots, square aspect container, recovery boundary | 20-ply/engine/layout/fullscreen browser tests | Board survives moves/engine changes; no page/console errors in passing tests | Catastrophic browser failure is outside React boundary |

## Offline, privacy, design, testing, and final evidence

| Requirement | Relevant file or subsystem | Current state | Planned fix | Test or manual verification | Evidence/result | Remaining limitation |
| --- | --- | --- | --- | --- | --- | --- |
| OFF-01: separate core/engine/OCR caches; optional model cannot break install | `sw.js`, cache client | Verified | Core install first; immutable engine cache; independent lazy OCR version; CORS-compatible precache and `Vary: Origin` fallback | SW source tests plus Cache Storage browser inspection | Three cache names/counts verified; cached entry JS/CSS load after HTTP-cache clear | Browser storage policy may evict optional data |
| OFF-02: explicit model download with progress/errors | scanner/offline cache helper | Verified | Cache each pinned OCR asset on user action and expose progress | Cache helper units and offline E2E | Download completes before network disable and remains available | Initial model download needs network unless release was previously cached |
| OFF-03: true offline reload after HTTP-cache clear; OCR + Stockfish + navigation | production PWA E2E | Verified | Use service-worker storage only after CDP HTTP-cache clear and network disable | `offline-ocr.spec.ts` | Exact reference OCR, legal Stockfish line, reload, scanner, and navigation pass offline in 19.7 s | Tested in Linux Chromium, not every Chrome platform version |
| PRIV-01: no screenshot/pixel/FEN upload | network monitoring | Verified | Local-only workers/storage; record cross-origin and non-read requests | OCR and offline request listeners | Zero external image/inference requests and zero POST/PUT uploads | Browser-extension traffic is outside page instrumentation |
| UI-01: serious board-dominant workstation, light/dark, focus, minimal motion | App/ScanPanel/CSS | Verified | Applied frontend-design pass after functional stabilization | Dark/light screenshot review and browser role/focus checks | Consistent scanner rails, contrast, controls, local-font-safe motif | Formal contrast tooling/full WCAG audit not run |
| UI-02: responsive/no overflow/square/fullscreen at required viewports and zoom | CSS and E2E matrix | Verified | Remove inline critical grids; full-width scanner; stack ≤1100; wrap ≤620; square constraints | 7 viewport + 4 zoom factors in core and scanner tests | 1920×1080 through 390×844 and 80/100/125/150% pass | OS text scaling beyond tested browser zoom is not separately measured |
| TEST-01: unit/integrity/service-worker/code-split/runner suites | Vitest/Node tests | Verified | Add output-based OCR, worker race, homography, FEN, history, PGN, storage and cache-mode tests | `npm test` | 13 Vitest files/85 tests plus 14 Node tests all pass | Unit tests do not replace browser evidence |
| TEST-02: complete Playwright suite including OCR | dynamic runner and three specs | Verified, with parent-process cap noted | Discover every test dynamically; run isolated processes for Lambda Chromium | Three complete spec files run separately | 16 OCR + 10 core + 1 cold-offline = 27/27 pass | Hosted long-parent cap requires per-file invocation; no assertion is omitted |
| PERF-01: scanner code split and measured model/inference/memory | Vite/benchmark | Verified | Lazy scanner import and worker timing/RSS instrumentation | Build chunk output; latest benchmark | Main 272.71 kB (86.95 gzip); scanner 51.86 kB (16.37 gzip); model load about 3.25 s; inference about 3.18–4.18 s in Node CPU run | Hardware/browser timings vary; precise browser peak heap unavailable |
| OUT-01: reports, README, notices, launchers, verified dist, final ZIP/diff summary | release tree | Verified | Package source/tests/docs/current `dist` without dependencies, agent tooling, or test temp data | ZIP integrity/extraction, excluded-path checks, duplicate asset hashes, clean install/build/server probes | Archive candidate: 147 files, 52 MB extracted; `unzip -t` clean; extracted launcher served HTML/WASM/model assets; final immutable ZIP hash is supplied in the handoff | The archive cannot contain its own SHA-256 without changing that hash |
